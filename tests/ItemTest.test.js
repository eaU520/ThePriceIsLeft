const { isEnabled } = require("react-native/Libraries/Performance/Systrace");
const { default: Item } = require("../components/Item");
const price1 = require("../components/Item");

// describe('Check price and name', () => {
//     it('price is 97 for a dozen eggs', () => {
//         expect(Item(.97, "Eggs")).toBe("Eggs is $.97");
//         //TODO: functional components and tests
//     });
    
    // it('multiply 4 * -3 to equal -12', () => {
    //     expect(multiply(4, -3)).toBe(-12);
    // });
// })